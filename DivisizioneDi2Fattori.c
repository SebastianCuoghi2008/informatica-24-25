/*Verificare se un numero dato in input è
divisibile sia per 3 sia per 5*/

#include <stdio.h>
int main (){
    int numero=0;

    printf("inserisci un numero a piacere");
    scanf("%d", &numero);

    if(numero /5 and /3){
        printf(il numero%d è divisibile sia per tre sia per cinque);
    }
    else{
        if(numero /5)
            printf("il numero %d è divisibile per cinque");
        else
            printf("il numero %d non è divisibile ne per cinque ne per tre");    
    }
    else{
        if(numero /3)
            printf("il numero %d è divisibile per tre");
        else
            printf("il numero %d non è divisibile ne per tre ne per cinque");
    }
    return 0;
}


        


    
