/*Data una sequenza di numeri (si termina non appena si inseriscono due numeri consecutivi uguali),
determinare il minimo e il massimo e verificare se uno è il multiplo/divisore dell’altro.*/

#include <stdio.h>

int main(){
    int numero1=0;
    int numero2=0;
    int Max1=0;
    int Max2=0;
    int Min1=0;
    int Min2=0;
    int divisore=0;
    int multiplo=0;

    printf("inserisci il primo nuuero: ");
    scanf("%d", numero1);
    printf("inserisci il secondo nuumero: ");
    scanf("%d", numero2);

    for(int i=0; i<numero1; i++){
        if(Max1<numero1){
            Max1==numero1;
        }
        if(Min1>numero1){
            Min1==numero1;
        }
    }
    for(int i=0; i<numero2; i++){
        if(Max2<numero2){
            Max2==numero2;
        }
        if(Min2>numero2){
            Min2==numero2;
        }
    }
    if("")
    
    }
    
    
        
    
    
    

    i



}