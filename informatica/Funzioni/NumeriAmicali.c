/*data una sequenza di valori, verificare se i valori a coppie si terminano quando uno dei valori è 0*/

#include <stdio.h>

int amicali(int *_N1, *_N2);

int main(){
    int N1=0;
    int N2=0;

    do{
        printf("inserisci due valori: ");
        scanf("%d%d", N1, N2);
    }while(N1==0 && N==2);
}
   

   